# Spalvos — window decoration
# Subtle 1px rounding and tight gaps, matching the quiet-paper look the
# palette is built for. User config sourced after the theme still wins.
general {
    gaps_in = 2
    gaps_out = 5
}
decoration {
    rounding = 1
}
